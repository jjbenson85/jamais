import { describe, it, expect } from "vitest";
import { signal } from "./signal";

describe("reactive", () => {
  it("should create a reactive object", () => {
    signal;
    // const r = createSignal({ test: 1 });
    // expect(r.test).toBe(1);
  });
});